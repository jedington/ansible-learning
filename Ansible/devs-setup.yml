--- # this is just an example
- hosts: devs
  become: yes
  tasks:
    - name: "install git"
      apt:
        name: git
        state: latest
    - name: "install vim"
      apt:
        name: vim
        state: latest
    - name: "setup dev folder"
      file:
        path: /code
        state: directory
    - name: "add dev group"
      group:
        name: developers
        state: present
